# EmailJS public key (required for sending emails)
# Get this value from https://dashboard.emailjs.com/admin/integration
VITE_EMAILJS_PUBLIC_KEY=your_emailjs_public_key_here

# All incoming form submissions will be delivered to the recipient email
# configured in `rfci/src/config/emailConfig.js` (info@rfcigh.com by default)
